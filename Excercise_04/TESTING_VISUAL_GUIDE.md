# 🎯 Visual Guide to React Testing

## Test Pyramid

```
         ▲
         │
      End-to-End Tests (Few)
      /                    \
     E2E (Cypress, Selenium)
    /                        \
   /                          \
  ┌────────────────────────────┐
  │                            │
  │  Integration Tests         │  ← You're here
  │  (RTL + Jest)              │
  │                            │
  └────────────────────────────┘
        / Many              \
       /                    \
      ┌──────────────────────┐
      │   Unit Tests        │
      │   (Jest)            │
      │   (Many)            │
      └──────────────────────┘
         │
         ▼
```

---

## Test Structure Flow

```
┌─────────────────────────────────────────────────┐
│            Test Execution Flow                  │
└─────────────────────────────────────────────────┘

1. ARRANGE (Setup)
   ├─ Import component
   ├─ Create mocks
   ├─ Render component
   └─ Get references to elements

2. ACT (Execute)
   ├─ User types in input
   ├─ User clicks button
   ├─ User presses keys
   └─ API call happens

3. ASSERT (Verify)
   ├─ Check element visible
   ├─ Check element disabled
   ├─ Check text content
   ├─ Check mock called
   └─ Check state changed
```

---

## LoginForm Component Flow

```
                    ┌──────────────────┐
                    │   LoginForm      │
                    │   Component      │
                    └────────┬─────────┘
                             │
                    ┌────────▼────────┐
                    │  Render Form    │
                    └────────┬────────┘
                             │
        ┌────────────────────┼────────────────────┐
        │                    │                    │
        ▼                    ▼                    ▼
    ┌─────────┐          ┌─────────┐          ┌──────────┐
    │ Email   │          │Password │          │ Submit   │
    │ Input   │          │ Input   │          │ Button   │
    └────┬────┘          └────┬────┘          └────┬─────┘
         │                    │                    │
         │  User types        │  User types       │ User clicks
         │  "user@..."        │  "pass..."        │
         │                    │                    │
         └────────────────────┼────────────────────┘
                              │
                    ┌─────────▼────────┐
                    │  Form.submit()   │
                    └─────────┬────────┘
                              │
                    ┌─────────▼────────┐
                    │ Call API:        │
                    │ /api/auth/login  │
                    └─────────┬────────┘
                              │
                  ┌───────────┴───────────┐
                  │                       │
            ┌─────▼──────┐          ┌────▼──────┐
            │  Success   │          │  Error    │
            │  Response  │          │  Response │
            └─────┬──────┘          └────┬──────┘
                  │                      │
        ┌─────────▼──────────┐  ┌────────▼────────┐
        │ Show "Welcome"     │  │ Show error msg  │
        │ Clear form         │  │ Keep form values│
        │ Call callback      │  │ Enable retry    │
        └────────────────────┘  └─────────────────┘
```

---

## MSW API Mocking Flow

```
Test Flow with MSW Mocking

┌──────────────────────────────┐
│  1. setupTests.js runs       │
│     server.listen()          │
│     MSW intercepts requests  │
└──────────────┬───────────────┘
               │
┌──────────────▼───────────────┐
│  2. Test starts              │
│     render(<LoginForm />)    │
└──────────────┬───────────────┘
               │
┌──────────────▼───────────────┐
│  3. User fills form          │
│     await userEvent.click()  │
└──────────────┬───────────────┘
               │
┌──────────────▼───────────────┐
│  4. Form calls:              │
│     fetch('/api/auth/login')  │
└──────────────┬───────────────┘
               │
    ┌──────────▼──────────┐
    │  MSW intercepts     │
    │  request            │
    └──────────┬──────────┘
               │
    ┌──────────▼──────────────────┐
    │  Check handler:             │
    │  rest.post('/api/...')      │
    └──────────┬──────────────────┘
               │
    ┌──────────▼──────────────────┐
    │  Return mock response:      │
    │  ctx.status(200)            │
    │  ctx.json({ user: {...} }) │
    └──────────┬──────────────────┘
               │
               │  (Not real network!)
               │
┌──────────────▼───────────────┐
│  5. Component gets response  │
│     Updates state            │
│     Shows success message    │
└──────────┬───────────────────┘
           │
┌──────────▼───────────────┐
│  6. Assert on DOM        │
│     expect(screen...) ✓  │
└──────────────────────────┘
```

---

## Error Boundary Protection

```
WITHOUT Error Boundary:
┌────────────────────────────┐
│      Your App              │
│  ┌──────────────────────┐  │
│  │  Parent Component    │  │
│  ├──────────────────────┤  │
│  │  ❌ Bomb Component   │  │ ← Throws error
│  │     (error thrown)   │  │
│  └──────────────────────┘  │
│  ┌──────────────────────┐  │
│  │  Sibling Component   │  │
│  └──────────────────────┘  │
└────────────────────────────┘
            │
            │ Error propagates
            │ up the tree
            ▼
      🤍 WHITE SCREEN 🤍
      (App completely crashes)


WITH Error Boundary:
┌──────────────────────────────────┐
│      Your App                    │
│  ┌────────────────────────────┐  │
│  │  <ErrorBoundary>           │  │
│  │  ┌──────────────────────┐  │  │
│  │  │ ❌ Bomb Component    │  │  │ ← Throws error
│  │  │    (error thrown)    │  │  │
│  │  └──────────────────────┘  │  │
│  │  ┌──────────────────────┐  │  │
│  │  │ Fallback UI shows    │  │  │
│  │  │ "Something wrong"    │  │  │
│  │  │ [Try again button]   │  │  │
│  │  └──────────────────────┘  │  │
│  └────────────────────────────┘  │
│  ┌────────────────────────────┐  │
│  │ Rest of app WORKS! ✅      │  │
│  └────────────────────────────┘  │
└──────────────────────────────────┘
        (App still responsive)
```

---

## Query Priority Guide

```
                    Query Priority
                         │
            ┌────────────────────────────┐
            │   Best: getByRole          │
            │   (Most semantic)          │
            │   Accessibility-focused    │
            └────────┬───────────────────┘
                     │
            ┌────────▼───────────────────┐
            │   Good: getByLabelText     │
            │   Text associations       │
            └────────┬───────────────────┘
                     │
            ┌────────▼───────────────────┐
            │   OK: getByText            │
            │   For non-interactive      │
            └────────┬───────────────────┘
                     │
            ┌────────▼───────────────────┐
            │   Last: getByTestId        │
            │   When nothing else works  │
            └────────────────────────────┘

Example:
┌─────────────────────────────────────┐
│ <form>                              │
│   <label htmlFor="email">Email</label>
│   <input id="email" />              │
│   <button>Submit</button>           │
│ </form>                             │
└─────────────────────────────────────┘

✅ Best:         screen.getByRole('button', { name: /submit/i })
✅ Good:         screen.getByLabelText('Email')
⚠️  OK:          screen.getByText('Submit')
❌ Avoid:        screen.getByTestId('submit-button')
```

---

## Async Testing Timeline

```
Test execution with async operations:

Time ──────────────────────────────────────────────────►

┌──────────────────────────────────────────────────────┐
│  render(<LoginForm />)                              │
└──────────────────┬───────────────────────────────────┘
                   │
        ┌──────────▼──────────┐
        │ userEvent.type()    │
        │ ✓ Completes        │
        └──────────┬──────────┘
                   │
        ┌──────────▼──────────┐
        │ userEvent.click()   │
        │ Triggers form       │
        │ submit...          │
        └──────────┬──────────┘
                   │
        ┌──────────▼──────────────────────┐
        │ fetch() called                  │
        │ (async operation starts)       │
        │ Returns Promise                │
        └──────────┬──────────┬──────────┘
                   │          │
          ┌────────┘          │
          │                   │
    ❌ WRONG:            ✅ RIGHT:
    ┌─────────────┐     ┌──────────────────┐
    │ Immediately │     │ await API call   │
    │ assert:     │     │ for completion:  │
    │             │     │                  │
    │ getByText() │     │ findByText() or  │
    │ ✗ Fails!   │     │ waitFor()        │
    │             │     │ ✓ Works!        │
    └─────────────┘     └────────────────┬─┘
                                 │
                        ┌────────▼────────┐
                        │ Assert on DOM   │
                        │ ✓ Element found │
                        └─────────────────┘

Key: Use 'find' or 'waitFor' for async operations!
```

---

## React Testing Library Selectors

```
┌──────────────────────────────────────────────────┐
│         All Selector Methods                     │
└──────────────────────────────────────────────────┘

Synchronous (no waiting):
├─ getByRole()              ✅ Best
├─ getByLabelText()         ✅ Good
├─ getByPlaceholder()       ⚠️  OK
├─ getByText()              ⚠️  OK
├─ getByDisplayValue()      ⚠️  OK
├─ getByAltText()           ⚠️  OK
├─ getByTestId()            ❌ Last resort
└─ queryByX()               (same but returns null)

Asynchronous (waiting):
├─ findByRole()             ✅ Best for async
├─ findByLabelText()        ✅ Good for async
├─ findByText()             ✅ For API results
├─ findByDisplayValue()     ⚠️  OK
├─ findByAltText()          ⚠️  OK
├─ findByTestId()           ❌ Avoid
└─ waitFor()                ✅ Complex conditions

Example Query:
┌────────────────────────────────────────────┐
│ screen.getByRole('button', {               │
│   name: /click me/i,    // Text content    │
│   exact: false,         // Partial match   │
│ })                                         │
└────────────────────────────────────────────┘
```

---

## Test Organization

```
Project Structure:

src/
├── components/
│   └── __tests__/          ← Tests near components
│       ├── Component.test.js
│       └── helpers.test.js
│
├── features/
│   └── auth/
│       ├── components/
│       ├── __tests__/      ← Feature tests
│       │   └── LoginForm.test.js
│       ├── hooks/
│       └── __tests__/
│           └── useAuth.test.js
│
└── __tests__/
    ├── mocks/              ← Shared mocks
    ├── fixtures/           ← Test data
    └── utils/              ← Test helpers

Benefits:
✅ Tests near code
✅ Easy to find tests
✅ Shared utilities
✅ Organized structure
```

---

## Common Test Patterns

### Pattern 1: Form Submission
```
render()
  ↓
userEvent.type() ← Input values
  ↓
userEvent.click() ← Submit
  ↓
await findByText() ← Wait for result
  ↓
expect() ← Assert
```

### Pattern 2: Error Handling
```
server.use(errorHandler) ← Override
  ↓
render()
  ↓
userEvent.click()
  ↓
await findByRole('alert') ← Error message
  ↓
expect(error).toHaveTextContent()
```

### Pattern 3: Async Operations
```
mock API called
  ↓
Data loading (async)
  ↓
await waitFor() ← Wait for condition
  ↓
expect() ← Assert final state
```

---

## Debugging Tips

```
┌─────────────────────────────────────────────┐
│  Debugging Checklist                       │
├─────────────────────────────────────────────┤
│ 1. screen.debug()                          │
│    └─ See what's rendered                  │
│                                             │
│ 2. console.log(mockFn.mock.calls)          │
│    └─ See mock call history                │
│                                             │
│ 3. getByRole('button') → getByText()       │
│    └─ Fallback to less specific            │
│                                             │
│ 4. Check MSW handlers                      │
│    └─ Verify handler is set up             │
│                                             │
│ 5. Use --inspect-brk for debugging         │
│    └─ Set breakpoints in DevTools          │
│                                             │
│ 6. Check for missing await                 │
│    └─ All async must be awaited            │
│                                             │
│ 7. Verify element exists first             │
│    └─ Use queryBy (returns null)           │
│                                             │
│ 8. Check testing-playground.com            │
│    └─ Get right selector                   │
└─────────────────────────────────────────────┘
```

---

## Coverage Metrics Explained

```
┌──────────────────────────────────────────────────┐
│  Code Coverage Types                            │
└──────────────────────────────────────────────────┘

Statement Coverage:
├─ How many statements executed?
├─ Each line of code counted
└─ 100% = every line executed

Branch Coverage:
├─ How many if/else paths taken?
├─ Both true and false branches
└─ 100% = all paths tested

Function Coverage:
├─ How many functions called?
├─ Every function definition
└─ 100% = every function used

Line Coverage:
├─ How many lines executed?
├─ Similar to statement
└─ 100% = every line ran

Example:
┌───────────────────────────┐
│ function greet(name) {    │
│   if (name) {       ← Branch
│     return 'Hi' + name;   │ ← Line
│   }                 ← Branch
│   return 'Hello';   ← Line
│ }                   ← Function
└───────────────────────────┘

Coverage Report:
├─ Statements: 80% (4/5 executed)
├─ Branches: 50% (1/2 paths taken)
├─ Functions: 100% (1/1 called)
└─ Lines: 80% (4/5 executed)
```

---

**Ready to visualize and understand React testing!** 📊
